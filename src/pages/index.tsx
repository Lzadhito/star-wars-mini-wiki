import type { NextPage } from "next";
import { css } from "@emotion/react";

const Home: NextPage = () => {
  return (
    <div
      css={css`
        background-color: blue;
      `}
    >
      soldier 76
    </div>
  );
};

export default Home;
