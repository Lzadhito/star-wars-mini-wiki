query get_film($id: ID) {
  film(id: $id) {
    id
    title
    openingCrawl
    director
    producers
    releaseDate
    speciesConnection {
      species {
        id
        name
      }
    }
    starshipConnection {
      starships {
        id
        name
      }
    }
    vehicleConnection {
      vehicles {
        id
        name
      }
    }
    characterConnection {
      characters {
        id
        name
      }
    }
    planetConnection {
      planets {
        id
        name
      }
    }
  }
}
