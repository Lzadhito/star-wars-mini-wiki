query get_all_vehicles {
  allVehicles {
    totalCount
    vehicles {
      id
      name
      model
      vehicleClass
      manufacturers
      costInCredits
      length
      crew
      passengers
      maxAtmospheringSpeed
      cargoCapacity
      consumables
    }
  }
}
