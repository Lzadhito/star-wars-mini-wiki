import { css } from "@emotion/react";

export const styGetAllLayout = css`
  column-count: 3;
  column-gap: 1em;

  .section {
    margin: 1rem 0;
  }

  .cardWrapper {
    cursor: pointer;
    display: inline-block;
    margin: 0 0 1em;
    width: 100%;
  }
`;
